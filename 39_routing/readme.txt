https://router.vuejs.org/installation.html#direct-download-cdn

npm install --save vue-router
[from within the source folder]
[As of npm 5.0.0, installed modules are added as a dependency by default, so the --save option is no longer needed. ]

we write the routes in routes.js seperated file

browse to:
http://localhost:8080/
http://localhost:8080/#/
http://localhost:8080/#/add
http://localhost:8080/add

# does not makes an additional request to the server (like anchor)


