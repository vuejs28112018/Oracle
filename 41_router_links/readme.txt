we could use <a href ="/add"></a>
but instead we prefer router-link and router-view to keep everything in the same page! [SPA]

we create a header.vue
and import it into the root

we add nice css to make it look like a menu

inspect items to see h ref